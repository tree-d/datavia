[workspace]
authors = ["Michael Berg <michael.berg@ufz.de>"]
channels = ["conda-forge"]
name = "datavia-pipelines"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = ">=3.14.0,<3.15"
gdal = ">=3.12.0,<4"

[pypi-dependencies]
datavia = { path = "../datavia/", editable = true }
